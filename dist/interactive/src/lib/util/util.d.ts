/**
 * creates a regex that matches one or many packages
 * @param packageNameOrPattern name or pattern (e.g. @nx/*) of the package
 */
export declare const getPackageRegex: (packageNameOrPattern: string) => string;
